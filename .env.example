# IMPORTANT: Do NOT use quotes in .env files for Docker!
# Docker doesn't strip quotes - they become part of the value

# Claude Code SDK OAuth Token
# Get this from: cat ~/.claude/.credentials.json | grep accessToken
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-YOUR-ACTUAL-TOKEN-HERE

# API Key for authentication (optional but recommended for REST API)
# Generate with: openssl rand -hex 32
CLAUDE_CODE_SDK_CONTAINER_API_KEY=your-api-key-here-no-quotes

# Resend API Key for magic link emails
# Get this from: https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key-here

# Email sender address (must be verified domain in Resend)
EMAIL_FROM=Claude CLI <noreply@your-domain.com>

# Session secret for JWT signing (generate with: openssl rand -hex 32)
SESSION_SECRET=your-session-secret-here-32-chars-minimum

# Port (optional, defaults to 8080)
# PORT=8080

# Email Access Control (optional)
# Comma-separated list of allowed email addresses
# ALLOWED_EMAILS=user1@company.com,user2@company.com,admin@company.com

# Comma-separated list of allowed email domains
# ALLOWED_DOMAINS=company.com,partner.org

# If neither ALLOWED_EMAILS nor ALLOWED_DOMAINS is set, any email address can request access